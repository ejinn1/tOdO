"use client";

import styled from "styled-components";

export default function LoginPage() {
  return <Wrapper>로그인 페이지</Wrapper>;
}

const Wrapper = styled.div`
  width: 100%;
  max-width: 60rem;
  height: 100%;
  background-color: white;
`;
